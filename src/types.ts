export interface Service {
  id: string;
  title: string;
  description?: string;
  longDescription?: string;
  icon?: string;
}

export interface Reason {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface Product {
  id: string;
  title: string;
  description?: string;
  longDescription?: string;
  icon?: string;
}
